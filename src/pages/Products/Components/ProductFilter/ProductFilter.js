import React from "react";
import styled from "styled-components";

const ProductFilter = () => {
  return <Wrapper>ProductFilter</Wrapper>;
};

export default ProductFilter;

const Wrapper = styled.div`
  width: 250px;
  max-width: 250px;
  padding-right: 0.5rem;
  padding-left: 0.5rem;
`;
